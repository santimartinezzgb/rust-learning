fn main() {
    println!("Hello, world!");
}

// rustc main.rs: Compiles the program and converts it to machine language
// It Generate an executable named 'main' (on Linux/Mac) or 'main.exe (on Windows)

// main: Programa ejecutable en lenguaje máquina que puede entender el procesador
// To run: ./main (on Linux/Mac) or main.exe (on Windows)

// Rust → Compilates once, the ejecutable run on every machines (unneed rust installed)
// Python/Ruby/JS → You need the interpreter always installed, but you run directly without compiling

// Trade-off: Rust require extra step (compilate) but it give portability and speed
